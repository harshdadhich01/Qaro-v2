import type { Metadata } from "next";
import { Mail, MapPin, MessageCircle } from "lucide-react";
import { PageHero } from "@/components/page-hero";
import { ContactForm } from "@/components/contact-form";
import { JsonLd } from "@/components/seo/json-ld";

export const metadata: Metadata = { title: "Contact QARO - Free Growth Consultation | Kota", description: "Book a free 30-minute growth consultation with QARO in Kota, Rajasthan. We will map your fastest growth path.", alternates: { canonical: "/contact" } };
export default function ContactPage() { const base = process.env.NEXT_PUBLIC_SITE_URL ?? "https://qaro.in"; const whatsapp = process.env.NEXT_PUBLIC_WHATSAPP_NUMBER; return <><PageHero eyebrow="Contact / Start here" title="Let's Talk About Growing Your Business." description="Tell us what is working, what feels stuck and what the next stage should look like. We will help make the first move clear." path="/contact" breadcrumbName="Contact" /><section className="section contact-layout"><div><span className="eyebrow">Direct contact</span><div className="contact-detail"><Mail /><div><span>Email</span><a href="mailto:hello@qaro.in">hello@qaro.in</a></div></div><div className="contact-detail"><MessageCircle /><div><span>WhatsApp</span><a href={whatsapp ? `https://wa.me/${whatsapp}` : "mailto:hello@qaro.in"}>{whatsapp ? "Start a conversation" : "Email us to start"}</a></div></div><div className="contact-detail"><MapPin /><div><span>Based in</span><p>Kota, Rajasthan, India</p></div></div><p className="contact-note">Free 30-minute consultation. No pitch deck. Just a clear conversation about the constraint and opportunity.</p></div><div><h2>Give us the useful context.</h2><ContactForm /></div></section><JsonLd data={{ "@context": "https://schema.org", "@type": "ContactPage", url: `${base}/contact`, name: "Contact QARO", about: { "@id": `${base}/#organization` } }} /></> }
